# Maintainer: artoo <flower_of_life@gmx.net>

pkgname=manjaro-tools
pkgver=0.9.1
pkgrel=1
pkgdesc='Tools for Manjaro Linux'
arch=('any')
license=('GPL')
url='https://github.com/udeved/manjaro-tools'
depends=('namcap' 'openssh' 'rsync')
provides=('devtools' 'arch-install-scripts')
conflicts=('devtools' 'arch-install-scripts')
backup=('etc/manjaro-tools/manjaro-tools.conf')
source=("${url}/archive/${pkgname}-${pkgver}.tar.gz")
sha256sums=('74b74bca19a27b4ba26999b80efaef8dcf729e224cc30dcb12be3c817e848191')

build() {
	cd ${srcdir}/${pkgname}-${pkgname}-${pkgver}
	make SYSCONFDIR=/etc PREFIX=/usr
}

package() {
	cd ${srcdir}/${pkgname}-${pkgname}-${pkgver}
	make SYSCONFDIR=/etc PREFIX=/usr DESTDIR=${pkgdir} install
}
