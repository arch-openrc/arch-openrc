# Maintainer: udeved <udeved@openrc4arch.site40.net>

pkgbase=virtual-openrc
pkgname=('virtual-cron-openrc' 'virtual-timed-openrc')
pkgver=0.1
pkgrel=1
arch=('any')
url="http://openrc4arch.site40.net"
license=('GPL2')
makedepends=('openrc-cron' 'openrc-timed')

package_virtual-cron-openrc() {
	pkgdesc="OpenRC crond virtual package"
	groups=('openrc-base')
	depends=('openrc-cron')
}

package_virtual-timed-openrc() {
	pkgdesc="OpenRC ntpd virtual package"
	groups=('openrc-misc')
	depends=('openrc-timed')
}
