# Maintainer: udeved <udeved@openrc4arch.site40.net>

_rn="avahi"

pkgname=openrc4arch-${_rn}-plugin
pkgver=1.1.1
pkgrel=1
pkgdesc="OpenRC ${_rn} init script"
arch=('any')
url="http://openrc4arch.site40.net"
license=('GPL2')
groups=('openrc4arch-desktop')
depends=('openrc4arch' "${_rn}")
install=${_rn}.install



source=("http://openrc4arch.site40.net/pool/archlinux/${_rn}-${_ver}.tar.xz")
http://avahi.org/download/avahi-0.6.31.tar.gz
package() {
	cd $srcdir/$_pn-$_pn-$pkgver/${_rn}
	make \
		SYSCONFDIR='/etc' \
		PREFIX='/usr' \
		LIBEXECDIR='/usr/lib/rc' \
		DESTDIR="$pkgdir" \
		install
}
