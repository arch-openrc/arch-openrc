svc[0]="avahi-dnsconfd"
svc[1]="avahi-daemon"

_rlvl="default"

post_install() {
	_cmd="rc-update add ${svc[0]} ${_rlvl}"
	echo "	==> run '${_cmd}'"
}

post_upgrade() {
	post_install "$1"
}

post_remove() {
	_cmd="rc-update del ${svc[0]} ${_rlvl}"
	echo "	==> run '${_cmd}'"
}
